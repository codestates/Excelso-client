import { createGlobalStyle } from "styled-components";

export const GolbalStyles = createGlobalStyle`
* {
  font-family: Cafe24SsurroundAir;
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}
`;
